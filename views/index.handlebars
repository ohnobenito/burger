<h1>Burger Time!</h1>

<h2>Burgers on the menu:</h2>

<ul>
  {{#each burgers}}
    {{#unless devoured}}
      {{> burgers/burger-block devoured=true}}
    {{/unless}}
  {{/each}}
</ul>

<h2>Wow, you were hungry!</h2>

<ul>
  {{#each burgers}}
    {{#if devoured}}
      {{> burgers/burger-block devoured=false}}
    {{/if}}
  {{/each}}
</ul>

<h2>Add a Burger</h2>
<form class="create-form">

  <div class="form-group">
    <label for="burg">Burger Name:</label>
    <input type="text" id="burg" name="name">
  </div>

  <div class="form-group">
    <label for="devoured">Already ate it?</label><br>
    <input type="radio" name="devoured" value="1" checked> Ate it!<br>
    <input type="radio" name="devoured" value="0"> Want it!
  </div>

  <button type="submit">Add Burger to the menu</button>
</form>
